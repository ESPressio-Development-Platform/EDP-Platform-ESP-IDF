# Internal API

Internal adapters map EDP result types to ESP-IDF/Mbed TLS status codes, translate memory-resource requests into heap capability masks, retain caller-supplied task backing, and perform segmented AAD processing without concatenation buffers.
