# Build, Test and Source Map

C++20 and ESP-IDF are required. `docs/` covers execution context, spinlock, randomness, AES-GCM and memory resources. Target tests/demos should validate both capability metadata and native behaviour under the selected ESP-IDF baseline.
